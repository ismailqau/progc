#ifndef _OPTIONS_H_
#define _OPTIONS_H_

/**
 * @brief           Entry point for calculating.
 * @param[in] text  Text to be processed.
 * @return          Calculated value.
 */
typedef struct {
    int min;
    int max;
    int step;
} options;

#endif